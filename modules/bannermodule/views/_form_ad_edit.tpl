{*
 * __XYZZY_BOILER
 * $Id$
 *}
<div class="form_title">{if $is_edit == 1}Edit Banner Ad{else}Create a Banner Ad{/if}</div>
<div class="form_header">
Currently, only image-based banners are supported.
</div>
{$form_html}